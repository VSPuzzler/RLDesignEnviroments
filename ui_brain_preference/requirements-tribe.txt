# Optional: real Tribe V2 backend.
#
# Tribe V2 is NOT on PyPI. Install Meta's package directly from GitHub:
#
#   git clone https://github.com/facebookresearch/tribev2 ../external/tribev2
#   pip install -e ../external/tribev2
#
# Constraints to be aware of (from Meta's pyproject.toml):
#   - Python >=3.11, <3.14  (the editable install pins torch<2.7, which has no
#     Python 3.14 wheel; use 3.11 or 3.12).
#   - torch>=2.5.1,<2.7
#   - HUGGING_FACE_HUB_TOKEN with access to gated meta-llama/Llama-3.2-3B.
#   - First inference downloads ~12 GB of weights.
#   - License: CC-BY-NC-4.0 (research / non-commercial only).
#
# See ui_brain_preference/brain_scorer.py for how the real model is invoked.

-r requirements.txt

torch>=2.5.1,<2.7
torchvision>=0.20,<0.22
transformers>=4.38.0
huggingface_hub>=0.21.0
