# ui_brain_preference — minimal install for the simulation-mode pipeline.
#
# This file installs everything needed to run `python main.py "..."` end-to-end
# WITHOUT a real Tribe V2 backend (brain_scorer.py falls back to deterministic
# simulation). For real Tribe V2, see requirements-tribe.txt.

# OpenRouter (uses the OpenAI-compatible client).
openai>=1.12.0

# Headless browser for screenshots.
playwright>=1.42.0

# .env autoloading (config.py looks for it; safe to omit).
python-dotenv>=1.0.0

# Brain-scorer simulation + image I/O.
numpy>=1.26.0
Pillow>=10.2.0
